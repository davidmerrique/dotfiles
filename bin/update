#!/bin/bash

sudo -v

while true; do sudo -n true; sleep 60; kill -0 "$$" || exit; done 2>/dev/null &

commands=(
  "rcup -d $DOTFILES"
  "brew update"
  "brew upgrade"
  "brew prune"
  "brew cleanup"
  "brew linkapps"
  "$DOTFILES/bin/nvmup"
  "npm update npm -g"
  "npm cache clean -g"
  "npm update -g"
  "gem update"
  "gem update --system"
)

echo "updating..."
for i in "${commands[@]}"; do
  printf "\n**** Running: $i *****\n\n"
  eval $i
done
